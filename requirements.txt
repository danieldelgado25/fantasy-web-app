# Canonical dependency declaration is pyproject.toml. Prefer:
#
#     pip install -e .              # pipeline only
#     pip install -e ".[webapp]"    # + Flask/serving layer (backend/)
#     pip install -e ".[dev]"      # + pytest
#     pip install -e ".[notebooks]" # + jupyter/matplotlib
#
# This flat list is kept for `pip install -r requirements.txt` out of habit;
# it mirrors pyproject's core `dependencies` plus the notebook/dev tools.
# Versions unpinned by project convention.
nflreadpy
polars
scikit-learn
jupyter
matplotlib
pytest
