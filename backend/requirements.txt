# Canonical dependency declaration is the repo-root pyproject.toml. Prefer
# installing from there so the pipeline package is picked up too:
#
#     pip install -e ".[webapp]"        # from the repo root
#     pip install -e ".[webapp,dev]"    # adds pytest for the test suites
#
# This flat list mirrors pyproject's [webapp] extra plus the pipeline's own
# runtime deps, for anyone installing from backend/ directly. It does NOT
# install the wr_predictor package itself — run `pip install -e .` for that.
# Versions unpinned by project convention.

# Web framework
flask
flask-cors

# Model loading / serving, and the polars<->pandas bridge used in
# projection_service (pyarrow backs polars.DataFrame.to_pandas()).
pandas
pyarrow
joblib

# Pipeline runtime deps (also declared as pyproject core dependencies).
polars
scikit-learn
nflreadpy
